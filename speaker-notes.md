# 毕业答辩讲稿备注

说明：当前 HTML 演示稿已按讲解内容重新组织论文图表。图表不是集中堆放在单独页面，而是放在对应的背景、方法、训练、实验、系统实现页面中，并配有对应的解读说明；保留的汇总页面用于把相关图表组织成可讲述的论证关系。

## 01 封面
各位老师好，我的论文题目是《面向语音与多模态伪造检测的安全评估方法与防护系统研究》。本文围绕深度伪造带来的语音隐私、视频内容安全和音视频联合伪造检测问题展开，并进一步做了语音安全硬件系统设计。

## 02 汇报提纲
汇报分为六部分：首先介绍研究背景和核心挑战，然后分别汇报三项算法工作，包括语音匿名化安全评测、视频伪造检测、音视频联合检测；接着介绍 ASV+ADD 语音安全系统，最后总结贡献和未来工作。

## 03 研究背景
深度生成模型推动了语音、视频和音视频同步伪造技术的发展。语音伪造更隐蔽，视频伪造更直观，音视频联合伪造则通过口型、声音和语义一致性提高可信度。这些技术带来身份欺骗、隐私泄露和内容安全风险。

## 04 核心挑战
本文关注四个挑战：匿名化之后身份信息是否真的被消除；视频伪造检测如何面对未知伪造类型和跨数据集场景；音视频联合伪造如何建模跨模态一致性；以及算法如何从实验室走向实际部署。

## 05 整体技术路线
本文的技术路线从语音安全评测出发，先做红队式攻击评估，随后扩展到视频检测，再到音视频联合检测，最后落到硬件防护系统。整体上形成从评测到检测再到部署的闭环。

## 06 论文主要贡献
四项工作分别对应四类问题。SpecWav-Attack 关注匿名语音身份残留；SFE-Net 关注视频伪造检测泛化；AV-LMMDetect 关注音视频联合伪造检测；ASV+ADD 系统关注语音安全的工程落地。

## 理论基础（一）：深度学习与特征表示
这一页对应论文第 2.1 节。先说明卷积神经网络通过局部连接和权值共享提取图像与视频中的空间结构特征，再说明注意力机制通过相关性建模、权重分配和特征聚合提升上下文建模能力。随后引出 Wav2Vec 2.0 的自监督语音表示，以及多模态大模型对图像、视频、音频和文本的统一建模能力。

## 理论基础（二）：语音信号处理与声纹识别
这一页对应论文第 2.2 节。梅尔频谱是从时域语音波形中提取声学特征的基础表示，ECAPA-TDNN 则通过 TDNN、Res2Net、通道注意力和注意力统计池化生成说话人嵌入。讲解时重点连接后续工作一的匿名语音安全评测和工作四的 ASV 模块。

## 理论基础（三）：伪造生成与检测原理
这一页对应论文第 2.3 节。GAN 和 Diffusion 是两类典型生成模型，它们提升伪造内容质量，也会在生物一致性、频率域、压缩重建、时序一致性和音视频同步等方面留下检测线索。讲解时可以自然过渡到后续的视频伪造检测和音视频联合伪造检测任务。

## 07 工作一问题
语音匿名化目标是在保留语义内容的同时隐藏说话人身份。但随着自监督语音模型的发展，匿名语音是否仍能在高维特征空间中被重新区分，是一个需要系统评估的问题。这项工作对应论文 SpecWav-Attack: Leveraging Spectrogram Resizing and Wav2Vec 2.0 for Attacking Anonymized Speech，ICASSP 2025 第一作者，并在 Voice attacker challenge 中取得全球前五。

## 08 工作一：SpecWav-Attack 方法框架
SpecWav-Attack 由五部分组成：输入匿名语音，使用 Wav2Vec 2.0 提取深层特征，引入频谱调整增强，再通过 ECAPA-TDNN 学习说话人嵌入，最后完成身份分类和安全评估。

## 09 工作一：频谱增强与训练策略
频谱调整在频率维度重采样，用来模拟匿名化造成的音色变化。训练采用两阶段策略：先在原始语音上建立基础判别能力，再用增强数据微调，使模型适应频域扰动。

## 10 工作一：实验设置
实验使用 LibriSpeech train-clean-360 作为训练集，dev 和 test 各包含 40 位说话人。评估指标是 EER。在这个场景下，EER 越低说明攻击模型越能从匿名语音中识别身份残留。

## 11 工作一：实验结果
从表格结果看，引入 SpecWav 后多个匿名条件下 EER 明显下降。例如 LibriSpeech-test 的 T10-2 条件从 40.36% 降到 26.54%。这说明频谱增强和自监督特征结合后，攻击模型更容易发现匿名语音中的身份残留。

## 12 工作一：小结
工作一的核心价值是提供了一个更强的匿名化安全评测工具。它说明匿名化系统不仅要看传统识别模型下是否安全，还要检查深层语音表示空间中是否仍存在身份可分性。

## 13 工作二问题
视频伪造检测的问题在于伪造痕迹非常多样。不同生成方法可能分别表现为光照异常、高频伪影、压缩残差、边界融合或者纹理异常。固定特征表达很难适应所有情况。这项工作对应论文 SFE-Net: Harnessing Biological Principles of Differential Gene Expression for Improved Feature Selection in Deep Learning Networks，ICASSP 2025 第一作者。

## 14 工作二：SFE-Net 核心思想
SFE-Net 借鉴差异基因表达机制。生物细胞在不同环境中会选择性激活不同基因；对应到检测任务，模型根据不同输入样本动态选择更重要的伪造特征。

## 15 工作二：五类互补特征
五类特征分别是光照一致性、高频、压缩重建、形态学操作和纹理特征。它们从不同角度刻画伪造痕迹，彼此互补，为后续动态选择提供候选特征集合。

## 16 工作二：网络结构
SFE-Net 先提取五类帧级特征，再通过门控机制生成每类特征的重要性权重，完成重标定和融合。视频层面再通过双向 LSTM 建模时序关系，最后完成真假分类。

## 17 工作二：实验设置
视频实验采用跨数据集协议，在 FaceForensics++ 上训练，在 Celeb-DF、DFD、DFDC 和 DFDCP 等数据集上测试。这样可以检验模型在不同数据分布和伪造类型下的泛化能力。

## 18 工作二：跨数据集结果
SFE-Net 在五个测试数据集上的平均 AUC 为 0.795，优于 Xception、UCF、SPSL、SRM 等对比方法。在 CDF-v1、CDF-v2 和 DFD 上优势尤其明显，说明动态特征选择提升了跨数据集泛化能力。

## 19 工作二：消融实验
消融实验显示，单一特征分支都有一定贡献，其中 Moop 和 Comr 较强。五类特征直接融合达到 0.771，引入 SFE-Net 后达到 0.795，说明多特征互补和动态选择机制都有效。

## 20 工作二：小结
工作二的核心结论是：面对复杂视频伪造场景，不应固定使用同一组特征，而应根据输入样本动态调整特征权重。这是 SFE-Net 相比传统固定特征方法的关键差异。

## 21 工作三问题
真实攻击中音频和视频可能同时被伪造。单模态检测只看音频或视频，无法充分利用口型同步、语义一致性、声音和表情匹配等跨模态线索，因此需要音视频联合检测。这项工作对应论文 Leveraging large multimodal models for audio-video deepfake detection: a pilot study，ICASSP 2026 共同第一作者。

## 22 工作三：AV-LMMDetect 框架
AV-LMMDetect 以 Qwen 2.5 Omni 为多模态基座，把视频帧、音频信号和检测问题一起输入模型，将伪造检测转化为多模态问答式判别任务，输出 Real 或 Fake。

## 23 工作三：联合输入
视频侧均匀采样 16 帧，音频侧提取对应片段的梅尔频谱。视觉 Token、音频 Token 和文本问题 Token 拼接成统一输入序列，让模型同时利用视觉、听觉和任务文本信息。

## 24 工作三：Top-2 Token 策略
为了避免生成完整文本带来的随机性，本文直接取 Real 和 Fake 两个 Token 的 logits，通过 Softmax 得到分类概率。若 p(Fake) 大于 0.5，则判定为伪造。

## 25 工作三：两阶段 SFT
第一阶段使用 LoRA 进行高效任务适配，冻结音频和视觉编码器，只更新少量参数。第二阶段解冻音频和视觉编码器，进一步学习底层伪造特征和跨模态异常。

## 26 工作三：FakeAVCeleb 结果
在 FakeAVCeleb 数据集上，AV-LMMDetect 的 AUC 达到 99.2%，Accuracy 为 98.02%。与 AVFF 等先进多模态方法相比，整体处于同一水平，并在 AUC 上略有提升。

## 27 工作三：MAVOS-DD 结果
在 MAVOS-DD 多语言开放集评估中，模型在 In-domain、Open-set model、Open-set language 和 Open-set full 四种场景下都保持较强性能。最困难的 Open-set full 场景下仍达到 mAP 0.96、AUC 0.92、Accuracy 85.09%。

## 28 工作三：小结
工作三说明多模态大模型不仅可以做通用理解，也可以通过任务适配用于伪造检测。它的优势在于能够同时捕获模态内伪影和模态间语义、时序一致性异常。

## 29 工作四问题
前面三项工作主要是算法层面，但实际语音安全还需要部署载体。纯软件方案可能被绕过，单独的 ASV 或 ADD 也不足以覆盖未授权用户和合成语音攻击，因此设计了双因素级联系统。

## 30 工作四：ASV+ADD 系统
ASV 模块回答“是谁在说”，ADD 模块回答“是不是人在真实说”。只有 ASV 为 Accept 且 ADD 为 Real 时，系统才允许音频通过，否则全部拒绝。

## 31 工作四：硬件实现
系统使用数字 MEMS 麦克风通过 I2S 采集语音，主控芯片完成预处理、特征提取和模型推理。模型以 INT8 量化形式部署，控制单元根据联合决策控制音频输出通路。

## 32 工作四：应用场景与指标
系统可用于智能语音助手、语音支付、智能门锁、车载语音和远程会议等场景。论文给出的测试指标包括 ASV EER 3.8%、ADD AUC 92.3%、攻击拒绝率 98.5%、端到端延迟小于 100ms、功耗小于 500mW。

## 33 全文总结
本文形成了从红队攻击评测到蓝队防御系统、从单模态到多模态、从算法到硬件实现的完整链条。总结时重点放在四项工作的技术关系：工作一评估匿名语音的身份残留，工作二提升视频伪造检测泛化，工作三扩展到音视频联合检测，工作四把身份认证和真伪检测落到语音采集前端。论文和专利成果放到下一页科研成果页单独讲。

## 科研成果
研究生期间的成果主要包括三篇 ICASSP 论文和一项发明专利。三篇论文分别是 SpecWav-Attack: Leveraging Spectrogram Resizing and Wav2Vec 2.0 for Attacking Anonymized Speech，ICASSP 2025 第一作者；SFE-Net: Harnessing Biological Principles of Differential Gene Expression for Improved Feature Selection in Deep Learning Networks，ICASSP 2025 第一作者；Leveraging large multimodal models for audio-video deepfake detection: a pilot study，ICASSP 2026 共同第一作者。竞赛方面，SpecWav-Attack 在 Voice attacker challenge 中取得全球前五。专利方面，“受话人认证与深度伪造检测级联语音隐私保护装置”已初审合格。

## 致谢
感谢尹建君老师、方雁雁老师在选题、技术路线和论文写作中的指导。感谢未来信息创新学院和课题组提供的科研平台，感谢实验室同学在算法讨论、实验复现、数据整理、系统调试和答辩准备中的帮助，也感谢家人朋友的理解与支持。

## 结束页
以上是我的答辩汇报，请各位老师批评指正。
